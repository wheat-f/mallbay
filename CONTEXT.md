# MallBay 门店经营上下文

本上下文描述门店管理端用于经营分析的核心业务语言，帮助页面、报表和后续架构讨论使用一致的含义。

## 经营分析

**Operational Report module**：面向门店经营判断的分析结果，包含筛选范围、核心指标、趋势、期间对比、洞察和分析明细。
_Avoid_: 统计页、数据看板（这些词不能表达分析结果和行动含义）

**主日期口径**：一次分析查询用于解释业务数据时间范围的主要日期规则，例如订单创建、预约、施工完工或结算日期。
_Avoid_: 查询日期、报表时间（过于含糊）

**成本完整度**：订单成本信息是否足以支持毛利判断的状态。
_Avoid_: 成本是否存在（不能表达部分完整和待补齐）

**待补齐**：当前数据不足以得出可靠的成本或毛利结论的状态。
_Avoid_: 按零计算、未知（前者会制造错误结论，后者没有行动含义）

**经营洞察**：由经营指标之间的关系识别出的、带有证据和下一步行动方向的异常或机会。
_Avoid_: 提示、提醒（不能表达分析依据）

**分析区状态**：摘要、期间对比、趋势、洞察或明细等报表结果区域当前是否可用，以及不可用或不完整的原因。
_Avoid_: 页面状态（过于宽泛）、空数据（不能区分没有记录和无法计算）

## 查询范围

**报表查询范围**：由门店、角色权限、主日期口径和日期区间共同确定的一次分析数据集合。
_Avoid_: 报表条件、筛选器（只描述界面，不描述业务含义）

**期间对比**：将当前报表查询范围与对应历史期间进行同口径比较的结果。
_Avoid_: 环比（只有在比较期间确实是相邻期间时才使用）

**在途订单**：已进入订单流程但尚未满足完工或质保完成条件的订单。
_Avoid_: 未完成订单（没有说明它是否仍然计入金额和待收分析）

**已完工订单**：已满足施工完工或业务规定的质保完成条件、可以进入完成口径分析的订单。
_Avoid_: 已结束订单（可能混淆取消、作废和完成）

**经营金额**：订单金额、实际收款和待收金额等反映订单商业价值或现金流的指标，可以包含在途订单。
_Avoid_: 已结算金额（会错误排除在途订单）

**完工指标**：要求订单满足已完工条件后才有业务意义的指标，例如完工率和基于完整成本的毛利率。
_Avoid_: 全量指标（会混淆在途订单和已完工订单）

## 订单履约

**订单履约闭环**：从订单创建、派工、施工、质检、最终交付到质保和售后的连续业务过程。
_Avoid_: 订单状态页（只描述界面，不表达跨模块事实）

**最终交付**：施工完工、质检通过、收款条件满足，并完成订单完成、质保和待办收口后形成的业务事实。
_Avoid_: 施工完成（施工记录完成不等于订单最终交付）

**履约能力**：当前用户针对订单可以执行或查看的动作集合，例如派工、开始施工、质检、最终交付和售后入口。
_Avoid_: 按钮权限（只描述界面，不表达业务条件）

**履约命令**：由具备相应履约能力的用户发起、经过前置事实校验并原子形成订单履约状态变化的业务动作，例如派工、开始施工、施工完工、质检、最终交付、取消和允许的回退。
_Avoid_: 状态更新（忽略权限、前置事实和原子业务结果）、按钮操作（只描述界面触发方式）

**履约待办**：由订单履约事实产生、必须由后续业务动作完成或关闭的责任记录，例如质检通过后仍需收取尾款；它与对应履约事实同步形成或收口。
_Avoid_: 通知消息（通知只负责触达，不代表业务义务本身）、页面提醒（只描述展示方式）

**履约版本**：订单履约事实每次成功变化后单调递增的序号，用于识别履约命令所依据的事实是否仍为最新。
_Avoid_: 更新时间（不能识别并发覆盖）、页面版本（只描述界面状态）

**履约命令标识**：标识一次履约业务意图的稳定值；同一标识的重复提交返回原业务结果，新标识表示新的履约动作。
_Avoid_: 请求编号（可能随网络重试变化）、内容哈希（无法区分内容相同的真实复检）

**权威履约结果**：订单履约闭环在读取或成功执行履约命令后返回的统一业务结果，包含当前阶段、履约能力、阻塞原因和履约版本；调用端不得根据原始状态自行补充另一套解释。
_Avoid_: 订单状态（只是一项事实，不能完整表达履约过程）、页面步骤（只是权威履约结果的展示）

**履约版本变更**：一次已提交业务事务使权威履约结果失效并推动履约版本单调递增的可追溯记录；同一事务改变多个相关事实仍只形成一次版本变化。
_Avoid_: 命令日志（外部事实也会推动版本）、更新时间（不能证明事实与版本同事务）

**履约核验单**：对历史或运行期跨事实矛盾形成的持久处置记录；开放期间阻断普通履约命令，只有相关事实按其所有权被纠正并复核通过后才能关闭。
_Avoid_: 数据修复脚本（只是处理手段）、异常日志（没有责任、状态和关闭条件）

## 库存与采购

**库存流水事实**：记录库存数量变化、批次、单位、来源和操作原因的不可静默覆盖事实。
_Avoid_: 当前库存数（无法解释数量如何产生）

**库存预留**：为订单或业务需求锁定可用库存但尚未出库的数量事实。
_Avoid_: 锁库标记（不能表达释放、出库和幂等关系）

**采购需求**：表达产品、数量和单位的补货需要，不直接表示价格或库存已增加。
_Avoid_: 采购单（采购需求和采购执行单据不是同一概念）

**收货事实**：采购货物实际进入某个仓库批次的数量、单位、成本和时间记录。
_Avoid_: 入库完成（不能表达部分收货和批次追溯）

**采购执行**：将采购需求编排为采购单、审批、部分收货和收货追踪的业务过程；采购执行本身不拥有库存数量事实。
_Avoid_: 采购入库（会把采购过程和库存事实混为一谈）

## 定价与成本

**定价决策**：在给定客户、车辆、产品、施工条件和规则版本下生成的价格与成本结果。
_Avoid_: 价格查询（不能表达规则版本和审批原因）

**规则版本**：一组可校验、可发布、可追溯的定价或施工成本规则集合。
_Avoid_: 当前配置（不能区分草稿、已发布和历史版本）

**价格/成本快照**：报价确认或订单创建时冻结的定价决策结果，后续规则变化不得回算该历史事实。
_Avoid_: 订单价格（不能表达来源和冻结时点）

## 客户与财务

**客户归属**：客户或车辆在门店、销售人员和企业关系中的当前责任范围。
_Avoid_: 客户负责人（可能混淆销售归属、门店归属和企业成员）

**企业结算**：围绕同一企业客户的多笔订单形成的对账、应收和收款编排，不拥有客户或订单生命周期。
_Avoid_: 企业客户付款（过于宽泛，不能表达跨订单对账）

**结算执行**：围绕企业结算形成对账单、订单分配、统一收款、确认和红冲的连续业务过程；它编排订单金额变化，但不直接拥有现金事实。
_Avoid_: 收款接口（只描述触发方式，不能表达对账与红冲过程）、现金入账（会把结算编排和现金事实混为一谈）

**客户消费概览**：用于反映客户经营价值的订单金额、实际收款和待收摘要，可以包含在途订单；它不等同于企业结算应收。
_Avoid_: 客户总消费（没有说明在途、收款和待收口径）

**企业结算应收**：在明确结算条件和结算期间内纳入对账的订单应收金额，可以与客户消费概览使用不同订单范围。
_Avoid_: 总应收（没有说明结算范围和期间）

**现金事实**：实际发生的收款、付款、退款、冲销及其来源和反向关系。
_Avoid_: 财务金额（无法区分应收、已收和实际现金流）

**施工证据**：证明施工阶段实际发生或完成的照片、材料、质检和现场记录；施工证据不单独等同于最终交付。
_Avoid_: 施工附件（无法表达证据的业务作用）

**事实发生日期**：某项业务事实实际发生的日期，例如收货、收款、施工或结算发生日期；分析查询必须使用与指标含义匹配的事实发生日期，不得因字段缺失静默回退。
_Avoid_: 更新时间（可能只是数据被修改的时间）

## 身份与权限

**访问主体**：发起一次业务读取或操作、并由当前有效身份与权限绑定决定其可见范围和可执行动作的用户；访问主体不等同于页面传入的岗位字段或历史 `isAuditor` 标记。
_Avoid_: actor（实现术语，不能说明有效身份、能力和范围的业务含义）、当前用户（没有表达权限计算后的访问范围）

**访问范围**：访问主体在一次业务读取或操作中可以查看或作用于的总部、门店和本人责任数据范围；它由有效权限绑定与访问请求共同确定，不等同于角色名称或页面筛选条件。
_Avoid_: scope（实现术语，不能说明组织范围与责任范围的业务含义）、数据权限（过于宽泛，不能区分总部、门店和本人）

**访问能力**：访问主体针对某类业务资源可以执行的动作集合，例如读取、提交、审核或支付；它来自有效权限绑定，不等同于页面按钮或角色名称。
_Avoid_: 按钮权限（只描述界面触发方式）、角色权限（把授权结果和权限配置身份混为一谈）

**总部管理员**：绑定在总部范围、负责总部治理入口和全局权限策略的用户；当前阶段由系统初始化账号承载，不开放通过页面新增或绑定其他总部成员。
_Avoid_: 审核员（仅描述旧字段 `isAuditor`，不能作为新的权限含义）

**总部范围绑定**：用户与角色在总部组织范围建立的有效关系，可覆盖总部治理所允许的全局数据和操作。
_Avoid_: 全局角色（没有说明组织绑定关系）

**门店范围绑定**：用户与角色在指定门店建立的有效关系，只覆盖该门店的数据和操作。
_Avoid_: 门店岗位（岗位是业务身份，绑定还必须包含组织范围）

**复合角色用户**：同时拥有总部范围绑定和一个或多个门店范围绑定的用户，例如同时拥有 `HQ_ADMIN/HQ` 与 `MANAGER/STORE`。
_Avoid_: 多身份账号（没有明确身份来自角色还是组织范围）

**总部管理员初始化账号**：由权限迁移脚本优先使用 `HQ_ADMIN_USERNAME`，未配置时按 `zhouluoren` → `xiaoming` 顺序选择首个已存在账号，并幂等补齐唯一总部管理员绑定；脚本不创建账号或处理密码。
_Avoid_: 默认管理员（没有说明创建来源和环境差异）

**证据追加记录**：对施工事实的后续修正通过新增记录、撤销关系或补充关系表达，不静默覆盖既有施工证据。
_Avoid_: 覆盖施工记录（会破坏质保、售后和审计追溯）
