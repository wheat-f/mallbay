# 订单履约由 OrderLifecycle 统一拥有最终交付

---
status: accepted
---

订单的最终交付是施工完工、质检通过、收款条件、质保生成/激活、订单状态更新和待办关闭共同形成的一个业务事实。由 `OrderLifecycle` 作为 deep module 统一提供阶段/能力查询和状态转换，最终交付在一个事务内完成；施工、质保、售后和页面不得各自推断或写入订单最终状态。保留现有模块作为内部 implementation，不拆微服务，也不以异步事件替代当前要求原子的最终交付事务。
